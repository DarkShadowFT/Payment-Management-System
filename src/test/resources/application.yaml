spring:
  liquibase:
    enabled: true
  datasource:
    url: jdbc:h2:mem:pms-test
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true